{# genreSearch.html.twig

	This file provies the template to allow a user to search for the name of a toy.
	It uses the form templage and then displays results of the search on the same page.
	The results are displayed as a table with the toys name and price.  The toy name
	has an embedded link to the page for the specific toy.
#}

{% include 'header.twig' %}
<style>
	table, tr, th, td { border: 1px solid black !important; }
	td, th {padding: .4em;}
		.id { text-align: center; }
		.name { text-align: center; }
		.price { text-align: right; }
</style>

<h1>Search</h1>

{{form_start(form)}}
{{form_widget(form)}}
{{form_end(form)}}

{% if results %}
	<table>
		<tr>
		<th class="toynum">Game</th>
		<th class="name">Name</th>
		<th class="price">Release Date</th>
		<th class="name">Reviews</th>
		<th class="price">Description</th>
		<th class="price">URL</th>
		</tr>
	{% for item in results %}
		<tr>
		<td class="toynum"><a href="/toystore/item/{{ item.game_ID }}">Details</a></td>
		<td class="name">{{ item.name }}</td>
		<td class="price">{{ item.release_date }}</td>
		<td class="price">{{ item.reviews }}</td>
		<td class="price">{{ item.description }}</td>
		<td class="price">{{ item.url }}</td>
		</tr>
	{% endfor %}
	</table>
{% endif %}

{% include 'footer.twig' %}